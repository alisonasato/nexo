namespace Nexo.Api;

/// <summary>
/// Nomes de ambiente usados em mais de um lugar. Existem como constante porque
/// ambiente comparado por string solta é erro que não aparece na compilação.
/// </summary>
public static class Ambientes
{
    public const string Testes = "Testes";
}
