# Endereço interno da API.
#
# Sem NEXT_PUBLIC_: só o servidor do Next usa. O navegador chama /api no
# mesmo domínio, e a reescrita em next.config.ts repassa para cá.
API_INTERNA=http://localhost:5240
